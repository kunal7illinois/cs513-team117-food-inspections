# Raw dataset - 313MB, exceeds GitHub's 100MB file limit anyway.
# Get it from Box instead (see DataLinks.txt).
/Chicago-Food-Inspection/

# Generic data dump folder, if anyone creates one - review before committing
# anything here manually if it's small enough to matter (<50MB).
/data/

# Full parsed-violations fact table (~200MB, regenerate locally with
# scripts/violations/parse_violations.py - see that folder's README).
# Includes a leftover partial file from a failed sandbox write; safe to delete.
/scripts/violations/output/

# OS / editor junk
.DS_Store
Thumbs.db
.ipynb_checkpoints/
__pycache__/
*.pyc

# OpenRefine local backups (not the exported history JSON - that's tracked)
*.openrefine-backup/
