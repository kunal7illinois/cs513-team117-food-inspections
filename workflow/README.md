# Workflow model

Drop the overall ("outer") workflow W1 and the inner/OpenRefine workflow W2 here.

- If using YesWorkflow: `Workflow.yw` (annotations) + generated `Workflow.gv` (Graphviz).
- If using another tool: a source file (e.g. `.pptx`) + a rendered `Workflow.pdf`.
- Inner workflow W2 (e.g. OpenRefine steps): use OR2YW (https://github.com/idaks/OR2YWTool) or
  describe visually another way.
