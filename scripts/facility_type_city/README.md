# Facility Type & City (owner: Zehra)

OpenRefine clustering to canonicalize the ~520 raw `Facility Type` values and ~89 raw `City`
values down to their canonical forms (see Phase-I report, Problems 1 and 2).

Output: two lookup tables (facility_type -> canonical, city -> canonical)
