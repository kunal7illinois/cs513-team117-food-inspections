# Violations (owner: Kunal)

Parse the pipe-delimited `Violations` field into individual rows, and remap violation codes so
they're consistent across the 07/01/2018 checklist change (see Phase-I report, Problem 4).

Output: `violation_finding_clean.csv`
